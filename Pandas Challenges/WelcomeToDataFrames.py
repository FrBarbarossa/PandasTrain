# Task condition
import pandas as pd

df = pd.DataFrame({'name': ['Jeff', 'Esha', 'Jia'],
                   'age': [30, 56, 8]})

first_name = df.name[0]

# Target: Define a variable expected_name with the value first_name will have.
expected_name = "Jeff"
